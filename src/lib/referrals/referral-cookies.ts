/** First-party cookie: last referral code captured for signup (non-secret). */
export const ACTIVE_REFERRAL_COOKIE = "doublle-active-referral-code";

/** 30-day attribution window (PRD). */
export const ACTIVE_REFERRAL_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;
